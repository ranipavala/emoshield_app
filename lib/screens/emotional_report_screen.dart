import 'package:flutter/material.dart';

class EmotionalReportScreen extends StatelessWidget {
  const EmotionalReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Emotional Report')),
      body: const Center(child: Text('Report UI Placeholder')),
    );
  }
}
